<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <p><%=@playlist.user.first_name%></p>
        <p><%=@playlist.song.title%></p>
        <p><%=@user.songs.count%></p>
        <!-- <%@songs.collect(&:title).uniq.each do |x|%>

            <ul>

            <li><%=x%></li>

            </ul>

        <%end%> -->
    </body>
</html>
